import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function operator_machine_dashboard() {
  return <ScreenPlaceholder title={"Machine Dashboard"}
  hint={"Machine dashboard - api.machines + api.jobCards (wiring in Phase 7)"} />;
}
