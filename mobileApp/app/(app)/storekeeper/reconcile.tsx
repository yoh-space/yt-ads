import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function storekeeper_reconcile() {
  return <ScreenPlaceholder title={"Reconcile"}
  hint={"Stock reconciliation (wiring in Phase 7)"} />;
}
