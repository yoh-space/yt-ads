import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function storekeeper_requests() {
  return <ScreenPlaceholder title={"Requests"}
  hint={"Material requests - live list (wiring in Phase 7)"} />;
}
