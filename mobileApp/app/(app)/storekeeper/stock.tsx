import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function storekeeper_stock() {
  return <ScreenPlaceholder title={"Stock"}
  hint={"Stock overview - api.parentInventory (wiring in Phase 7)"} />;
}
