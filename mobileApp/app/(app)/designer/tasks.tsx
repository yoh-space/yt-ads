import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function designer_tasks() {
  return <ScreenPlaceholder title={"Tasks"}
  hint={"Designer task queue (wiring in Phase 7)"} />;
}
