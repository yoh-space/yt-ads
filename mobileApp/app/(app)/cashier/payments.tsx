import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function cashier_payments() {
  return <ScreenPlaceholder title={"Payments"}
  hint={"Payment verify - api.cashier.orders (wiring in Phase 7)"} />;
}
